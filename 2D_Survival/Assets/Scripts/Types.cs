using UnityEngine;
using System.Collections;

public enum TileVertices { topLeft, topRight, bottomRight, bottomLeft };

public enum RenderUpdateOperations { CREATE, DESTROY, UPDATE_POSITION, UPDATE_BRIGHTNESS, UPDATE_TINT, UPDATE_TRANSPARENCY, UPDATE_ANIMATION };

public enum RenderLayers { TERRAIN, WORLD, HUD, UI };

public class Types {
	
}
